#ifndef SHOW_HPP
#define SHOW_HPP 1

#include "../core.hpp"

namespace show {
    extern void runShow(const gen::flagNames &options, const std::map<std::string, gen::classData> &myClasses);
};

#endif